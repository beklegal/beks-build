# Example environment variables for local development
# Copy this file to `.env.local` (or `.env`) and add your secret values there.
# Do NOT commit secrets to version control.

# Resend API key (required in production to send emails)
RESEND_API_KEY=

# Email to receive contact form submissions (defaults to b.e.koranteng@gmail.com)
CONTACT_EMAIL=

# Optional: override port
PORT=3000
